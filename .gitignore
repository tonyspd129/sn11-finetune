# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# venv
.venv/
venv/

benchmark

room

# sandbox run artifacts: output/<timestamp>/<task>/{result.json, work/}
output/

# built once on the host, injected into containers via PYTHONPATH (pytest + deps)
.verifier-lib/

# secrets — real env (keys); .env.example IS tracked
.env
.env.local
