{
  "name": "web_fetch",
  "description": "Fetch an http/https URL and return its readable text content (truncated).",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {"type": "string"}
    },
    "required": ["url"]
  },
  "version": "1.0"
}
