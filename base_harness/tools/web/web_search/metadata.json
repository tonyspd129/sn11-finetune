{
  "name": "web_search",
  "description": "Search the web for a query. Returns top results.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {"type": "string"},
      "num": {"type": "integer", "default": 5}
    },
    "required": ["query"]
  },
  "version": "1.0"
}
