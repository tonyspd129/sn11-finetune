{
  "name": "search",
  "description": "Search files for a regex pattern. Returns file:line matches, capped.",
  "parameters": {
    "type": "object",
    "properties": {
      "pattern": {"type": "string"},
      "path": {"type": "string", "description": "Dir/file to search (default cwd).", "default": "."},
      "max_results": {"type": "integer", "default": 50}
    },
    "required": ["pattern"]
  },
  "version": "1.0"
}
